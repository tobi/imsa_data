[project]
name = "imsa-sqlmesh"
version = "0.1.0"
description = "SQLMesh-based data transformation pipeline for IMSA endurance racing data"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "sqlmesh[duckdb]>=0.100.0",
]

[tool.uv]
dev-dependencies = []
